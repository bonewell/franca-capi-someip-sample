package ford.rnd

import ford.rnd.* from "ML.fidl"

interface SmartML extends ML {
  version { major 1 minor 0 }

  method ping fireAndForget {
    in {
      Int16 currency
    }
  }

  broadcast throwOut {
    out {
      String phone
    }
  }
}
